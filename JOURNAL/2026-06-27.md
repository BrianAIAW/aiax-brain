# 2026-06-27 — Saturday

## Summary
Big infrastructure day. Stripe connected (test), customer-number system verified, client portal rebuilt, knowledge brain created.

## What happened
- **Stripe (test mode)** connected to the Superagent app. Secret key stored securely as `$STRIPE_SECRET_KEY`. Live billing still requires owner to claim the real account in App Settings → Integrations → Stripe.
- **Customer number system** confirmed working: format `AIAX-####`.
  - Summit Ridge Dental → `AIAX-0001`
  - Harbor Point Law → `AIAX-0002`
- **Client Portal rebuilt** → now live at `aiax-portal-pro.base44.app` (app ID `6a3f4797d3da0d015cea35de`). Supersedes the older client portal.
- **Admin Portal** confirmed live at `aiax-admin-portal.base44.app` (app ID `6a3fdd2a73a6ab1de4376cea`).
- **Skills installed:** stripe-best-practices, pdf, docx, notion-research-documentation.
- **Knowledge brain created** — this repo `aiax-brain` set up as an Obsidian vault, to be synced via Chappie (OpenClaw).

## Security
- Established rule: secrets are never typed in chat — always entered via the secure secret form.

## Next up
- [ ] Generate sample branded PDF invoice for Summit Ridge Dental (show `AIAX-0001`).
- [ ] Wire Chappie to pull this repo into the Obsidian vault.
- [ ] Decide on real Stripe go-live timing.

## Links
- [[SYSTEMS/app-lineup]] · [[SYSTEMS/customer-numbers]] · [[SYSTEMS/stripe-billing]]

## Document engine — first branded invoice
Generated the first real branded PDF invoice (reportlab) end-to-end from LIVE Admin Portal data.
- Customer: Summit Ridge Dental (AIAX-0001), 6 active services, total $2,344.00/mo.
- Customer number flows straight from the live record onto the invoice — matches the portals.
- AIAX-branded (logo, navy/blue palette, payment terms, dual-branded footer).
- Next: wire into a reusable invoice skill so any customer's invoice generates on demand.

## Reusable invoice skill shipped
Turned the one-off invoice into a reusable skill: `.agents/skills/aiax-invoice/`.
- Pure PDF renderer driven by a JSON spec; AIAX logo bundled, auto-installs deps.
- SKILL.md documents the full workflow: pull live data from Admin Portal (app 6a3fdd2a…), build spec, render, deliver.
- Proven on a 2nd customer (AIAX-0002): 4 active services, total $1,146.00/mo.
- Now: "invoice AIAX-0001" → pulls live active services → branded PDF, on demand.

## Self-serve invoicing in Admin Portal
Built a portal-native invoice generator so any team member can bill with one click — no agent needed.
- New backend function `generateInvoice` in the Admin Portal (app 6a3fdd2a…): takes customer_id or customer_number, pulls that customer's ACTIVE subscriptions, renders a branded PDF (navy/blue, dual-branded), returns a download URL.
- 'Generate Invoice' button on each customer (detail page + Customers table row action).
- Verified live: AIAX-0001 = $2,344/mo, AIAX-0002 = $1,146/mo — matches the agent-side skill exactly.
- Two parallel paths now exist: portal button (self-serve) + agent skill ("invoice AIAX-0001").

## Service explainer pages — portal + website
Added customer-facing 'What is [Service]?' education across both surfaces.
- Customer Portal (app 6a3f4797…): each of the 9 service bubbles + service pages now link to a dedicated in-portal explainer page (What it is / What AIAX does / How it benefits you) with a 'Learn more on our website →' link out to the matching public page.
- Website: enriched seo.html with a 'SEO Questions, Answered' FAQ section + FAQPage schema (geo.html already had one). More indexable Q&A = better Google rich results + AI-answer visibility. Deployed live to Cloudflare (clean URLs /seo /geo verified 200).
- Canonical copy saved to /projects/SERVICE_EXPLAINERS.md (source of truth for portal + site).
- Also created Brian's owner login (brian@aiagentworx.ai) in the Client Portal with all 9 services active (note: portal assigned AIAW-000002; customer-number format differs from Admin's AIAX-#### — flagged to standardize).

## Customer Onboarding System (Client Portal)
Built a full self-serve onboarding system in the Client Portal (app 6a3f4797…).
- New entity OnboardingSubmission (customer_id, customer_number, form_type, status, answers_json, completion_percent, submitted_at, reviewed_by). One record per form per customer; supports save-draft + submit + completion %.
- 'Onboarding' nav item + Onboarding Hub page: master 'Company Profile' form (always shown) + a card per service the customer has, each with status badge + progress.
- 10 dynamic forms total: master Company Profile + one tailored form per service (SEO, GEO, Lead Gen, Paid Ads, AI Phone, Scheduling, CRM, Workflow, Website Design). Full question sets saved in /projects/ONBOARDING_FORMS_SPEC.md (source of truth).
- AI Onboarding Assistant (uses built-in InvokeLLM): hand-holds customers through the forms. Knows AIAW + the 9 services + each question's purpose, AND reads the customer's completed Company Profile answers as context to pre-draft service-form answers (Perplexity-style). Can explain questions, draft answers, 'Insert this answer' into a field; customer always reviews before submit.
- Plan: customers self-serve in Client Portal; admin reviews submissions read-only in Admin Portal (next step).

## Portal AI Assistant renamed → "WorX" (pronounced "works")
Brian renamed the portal AI assistants from Chatz / AIAX Assistant to **WorX** across the Client Portal (app 6a3f4797…). WorX = "Your Ai Agent Worx assistant"; recognizes WorX/Worx/worx.
- components/portal/OnboardingAssistant.jsx — floating button, panel header ("WorX — Your Ai Agent Worx assistant"), greeting, system prompt all updated to WorX.
- components/portal/AiaxAssistant.jsx — main portal assistant: button "Ask WorX", header WorX, greeting WorX.
- agents/aiax_assistant.json — agent description + full system instructions rewritten: identify as WorX, context-aware behavior for Dashboard / Active Service / Inactive Service views, plain-English benefit-driven voice.
- pages/OnboardingHub.jsx — added 3-step instructions banner + dismissible red warning to complete Company Profile before service forms; banner references WorX.
NOTE: 'WorX' is now the canonical name of the portal assistant. Use it going forward.